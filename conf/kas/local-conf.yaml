header:
  version: 1
local_conf_header:
  meta-sca: |
    SCA_VERBOSE_OUTPUT = "1"
    # for yocto versions > dunfell use "2"
    #CONF_VERSION = "2"
    # for yocto versions <= dunfell use "1"
    CONF_VERSION = "1"

  meta-sca-rm-work: |
    INHERIT += "rm_work"
